/* This file was generated by ODB, object-relational mapping (ORM)
 * compiler for C++.
 */

DROP TABLE IF EXISTS `tbl_user_like_meta`;

DROP TABLE IF EXISTS `tbl_video_meta`;

DROP TABLE IF EXISTS `tbl_user_follow_meta`;

DROP TABLE IF EXISTS `tbl_role_operation_meta`;

DROP TABLE IF EXISTS `tbl_user_identity_role_meta`;

DROP TABLE IF EXISTS `tbl_user_meta`;

DROP TABLE IF EXISTS `tbl_operation_meta`;

DROP TABLE IF EXISTS `tbl_role_meta`;

DROP TABLE IF EXISTS `tbl_identity_meta`;

DROP TABLE IF EXISTS `tbl_file_meta`;

DROP TABLE IF EXISTS `tbl_session_meta`;

CREATE TABLE `tbl_session_meta` (
  `primaryKey` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
  `session_id` VARCHAR(128) NOT NULL,
  `user_id` VARCHAR(128) NULL,
  `upload_time` BIGINT UNSIGNED NOT NULL DEFAULT 0 NOT NULL)
 ENGINE=InnoDB;

CREATE UNIQUE INDEX `session_id_i`
  ON `tbl_session_meta` (`session_id`);

CREATE INDEX `user_id_i`
  ON `tbl_session_meta` (`user_id`);

CREATE INDEX `session_id_idx`
  ON `tbl_session_meta` (`session_id`);

CREATE INDEX `user_id_idx`
  ON `tbl_session_meta` (`user_id`);

CREATE TABLE `tbl_file_meta` (
  `primaryKey` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
  `file_id` VARCHAR(128) NOT NULL,
  `upload_user_id` VARCHAR(128) NOT NULL,
  `path` VARCHAR(512) NULL,
  `size` BIGINT UNSIGNED NOT NULL,
  `mime_type` VARCHAR(64) NULL,
  `upload_time` BIGINT UNSIGNED NOT NULL DEFAULT 0 NOT NULL,
  `file_status` INT NOT NULL)
 ENGINE=InnoDB;

CREATE UNIQUE INDEX `file_id_i`
  ON `tbl_file_meta` (`file_id`);

CREATE INDEX `upload_user_id_i`
  ON `tbl_file_meta` (`upload_user_id`);

CREATE INDEX `file_id_idx`
  ON `tbl_file_meta` (`file_id`);

CREATE INDEX `upload_user_id_idx`
  ON `tbl_file_meta` (`upload_user_id`);

CREATE TABLE `tbl_identity_meta` (
  `primaryKey` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
  `identity_id` VARCHAR(128) NOT NULL,
  `identityType` TINYINT UNSIGNED NOT NULL,
  `identity_description` VARCHAR(64) NULL)
 ENGINE=InnoDB;

CREATE INDEX `identity_type_idx`
  ON `tbl_identity_meta` (`identityType`);

CREATE TABLE `tbl_role_meta` (
  `primaryKey` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
  `role_id` VARCHAR(128) NOT NULL,
  `role_type` TINYINT UNSIGNED NOT NULL,
  `role_description` VARCHAR(64) NULL)
 ENGINE=InnoDB;

CREATE INDEX `role_type_idx`
  ON `tbl_role_meta` (`role_type`);

CREATE TABLE `tbl_operation_meta` (
  `primaryKey` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
  `operation_id` VARCHAR(128) NOT NULL,
  `operation_url` VARCHAR(255) NOT NULL,
  `operation_description` VARCHAR(64) NULL)
 ENGINE=InnoDB;

CREATE INDEX `operation_url_idx`
  ON `tbl_operation_meta` (`operation_url`);

CREATE TABLE `tbl_user_meta` (
  `primaryKey` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
  `user_id` VARCHAR(128) NOT NULL,
  `bind_email` VARCHAR(64) NOT NULL,
  `user_name` VARCHAR(64) NOT NULL,
  `administrator_name` VARCHAR(64) NULL,
  `password` VARCHAR(64) NULL,
  `head_image_file_id` VARCHAR(512) NULL,
  `user_description` VARCHAR(128) NULL,
  `user_status` TINYINT UNSIGNED NOT NULL,
  `upload_time` BIGINT UNSIGNED NOT NULL DEFAULT 0 NOT NULL)
 ENGINE=InnoDB;

CREATE INDEX `user_id_idx`
  ON `tbl_user_meta` (`user_id`);

CREATE INDEX `bind_email_idx`
  ON `tbl_user_meta` (`bind_email`);

CREATE INDEX `user_name_idx`
  ON `tbl_user_meta` (`user_name`);

CREATE INDEX `administrator_name_idx`
  ON `tbl_user_meta` (`administrator_name`);

CREATE INDEX `user_status_idx`
  ON `tbl_user_meta` (`user_status`);

CREATE TABLE `tbl_user_identity_role_meta` (
  `primaryKey` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
  `user_id` VARCHAR(128) NOT NULL,
  `role_type` TINYINT UNSIGNED NOT NULL,
  `identity_type` TINYINT UNSIGNED NOT NULL)
 ENGINE=InnoDB;

CREATE INDEX `user_id_idx`
  ON `tbl_user_identity_role_meta` (`user_id`);

CREATE TABLE `tbl_role_operation_meta` (
  `primaryKey` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
  `role_type` TINYINT UNSIGNED NOT NULL,
  `operation_url` VARCHAR(128) NOT NULL)
 ENGINE=InnoDB;

CREATE INDEX `operation_id_idx`
  ON `tbl_role_operation_meta` (`operation_url`);

CREATE UNIQUE INDEX `role_op_unique_idx`
  ON `tbl_role_operation_meta` (
    `role_type`,
    `operation_url`);

CREATE TABLE `tbl_user_follow_meta` (
  `primaryKey` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
  `user_id` VARCHAR(128) NOT NULL,
  `follow_user_id` VARCHAR(128) NOT NULL)
 ENGINE=InnoDB;

CREATE INDEX `follow_user_id_idx`
  ON `tbl_user_follow_meta` (`follow_user_id`);

CREATE UNIQUE INDEX `user_follow_unique_idx`
  ON `tbl_user_follow_meta` (
    `user_id`,
    `follow_user_id`);

CREATE TABLE `tbl_video_meta` (
  `primaryKey` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
  `video_id` VARCHAR(128) NOT NULL,
  `video_file_id` VARCHAR(128) NOT NULL,
  `video_cover_file_id` VARCHAR(128) NOT NULL,
  `upload_user_id` VARCHAR(128) NOT NULL,
  `review_user_id` VARCHAR(128) NOT NULL,
  `video_name` VARCHAR(128) NOT NULL,
  `video_description` TEXT NULL,
  `video_play_count` INT UNSIGNED NOT NULL DEFAULT 0,
  `video_size` BIGINT UNSIGNED NOT NULL,
  `video_duration` BIGINT UNSIGNED NOT NULL,
  `video_upload_time` BIGINT UNSIGNED NOT NULL,
  `video_status` TINYINT UNSIGNED NOT NULL DEFAULT 0)
 ENGINE=InnoDB;

CREATE UNIQUE INDEX `video_id_i`
  ON `tbl_video_meta` (`video_id`);

CREATE INDEX `video_id_idx`
  ON `tbl_video_meta` (`video_id`);

CREATE INDEX `upload_user_id_idx`
  ON `tbl_video_meta` (`upload_user_id`);

CREATE INDEX `review_user_id_idx`
  ON `tbl_video_meta` (`review_user_id`);

CREATE INDEX `video_name_idx`
  ON `tbl_video_meta` (`video_name`);

CREATE INDEX `video_status_idx`
  ON `tbl_video_meta` (`video_status`);

CREATE TABLE `tbl_user_like_meta` (
  `primaryKey` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
  `user_id` VARCHAR(128) NOT NULL,
  `video_id` VARCHAR(128) NOT NULL)
 ENGINE=InnoDB;

CREATE INDEX `video_id_idx`
  ON `tbl_user_like_meta` (`video_id`);

CREATE UNIQUE INDEX `user_like_unique_idx`
  ON `tbl_user_like_meta` (
    `user_id`,
    `video_id`);

