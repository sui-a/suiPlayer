/* This file was generated by ODB, object-relational mapping (ORM)
 * compiler for C++.
 */

DROP TABLE IF EXISTS `tbl_file_meta`;

DROP TABLE IF EXISTS `tbl_session_meta`;

CREATE TABLE `tbl_session_meta` (
  `primaryKey` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
  `session_id` VARCHAR(128) NOT NULL,
  `user_id` VARCHAR(128) NULL,
  `upload_time` BIGINT UNSIGNED NOT NULL DEFAULT 0 NOT NULL)
 ENGINE=InnoDB;

CREATE UNIQUE INDEX `session_id_i`
  ON `tbl_session_meta` (`session_id`);

CREATE INDEX `user_id_i`
  ON `tbl_session_meta` (`user_id`);

CREATE TABLE `tbl_file_meta` (
  `primaryKey` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
  `file_id` VARCHAR(128) NOT NULL,
  `upload_user_id` VARCHAR(128) NOT NULL,
  `path` VARCHAR(512) NULL,
  `size` BIGINT UNSIGNED NOT NULL,
  `mime_type` VARCHAR(64) NULL,
  `upload_time` BIGINT UNSIGNED NOT NULL DEFAULT 0 NOT NULL,
  `file_status` INT NOT NULL)
 ENGINE=InnoDB;

CREATE UNIQUE INDEX `file_id_i`
  ON `tbl_file_meta` (`file_id`);

CREATE INDEX `upload_user_id_i`
  ON `tbl_file_meta` (`upload_user_id`);

