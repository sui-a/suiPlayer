// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "data-odb.hxx"

#include <cassert>
#include <cstring>  // std::memcpy

#include <odb/schema-catalog-impl.hxx>

#include <odb/mysql/traits.hxx>
#include <odb/mysql/database.hxx>
#include <odb/mysql/transaction.hxx>
#include <odb/mysql/connection.hxx>
#include <odb/mysql/statement.hxx>
#include <odb/mysql/statement-cache.hxx>
#include <odb/mysql/simple-object-statements.hxx>
#include <odb/mysql/view-statements.hxx>
#include <odb/mysql/container-statements.hxx>
#include <odb/mysql/exceptions.hxx>
#include <odb/mysql/simple-object-result.hxx>
#include <odb/mysql/view-result.hxx>
#include <odb/mysql/enum.hxx>

namespace odb
{
  // suiSessionMeta
  //

  struct access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      mysql::connection&,
      image_type&,
      id_image_type&,
      mysql::binding&,
      mysql::binding&)
    {
    }
  };

  access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::id_type
  access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  id (const id_image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          long long unsigned int,
          mysql::id_ulonglong >::set_value (
        id,
        i.id_value,
        i.id_null);
    }

    return id;
  }

  access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::id_type
  access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  id (const image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          long long unsigned int,
          mysql::id_ulonglong >::set_value (
        id,
        i._primaryKey_value,
        i._primaryKey_null);
    }

    return id;
  }

  bool access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // _primaryKey
    //
    t[0UL] = 0;

    // _session_id
    //
    if (t[1UL])
    {
      i._session_id_value.capacity (i._session_id_size);
      grew = true;
    }

    // _user_id
    //
    if (t[2UL])
    {
      i._user_id_value.capacity (i._user_id_size);
      grew = true;
    }

    // _upload_time
    //
    t[3UL] = 0;

    return grew;
  }

  void access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    std::size_t n (0);

    // _primaryKey
    //
    if (sk != statement_update)
    {
      b[n].buffer_type = MYSQL_TYPE_LONGLONG;
      b[n].is_unsigned = 1;
      b[n].buffer = &i._primaryKey_value;
      b[n].is_null = &i._primaryKey_null;
      n++;
    }

    // _session_id
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i._session_id_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i._session_id_value.capacity ());
    b[n].length = &i._session_id_size;
    b[n].is_null = &i._session_id_null;
    n++;

    // _user_id
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i._user_id_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i._user_id_value.capacity ());
    b[n].length = &i._user_id_size;
    b[n].is_null = &i._user_id_null;
    n++;

    // _upload_time
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i._upload_time_value;
    b[n].is_null = &i._upload_time_null;
    n++;
  }

  void access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  bind (MYSQL_BIND* b, id_image_type& i)
  {
    std::size_t n (0);
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i.id_value;
    b[n].is_null = &i.id_null;
  }

  bool access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  init (image_type& i,
        const object_type& o,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    bool grew (false);

    // _primaryKey
    //
    if (sk == statement_insert)
    {
      long long unsigned int const& v =
        o._primaryKey;

      bool is_null (false);
      mysql::value_traits<
          long long unsigned int,
          mysql::id_ulonglong >::set_image (
        i._primaryKey_value, is_null, v);
      i._primaryKey_null = is_null;
    }

    // _session_id
    //
    {
      ::std::string const& v =
        o._session_id;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i._session_id_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i._session_id_value,
        size,
        is_null,
        v);
      i._session_id_null = is_null;
      i._session_id_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i._session_id_value.capacity ());
    }

    // _user_id
    //
    {
      ::odb::nullable< ::std::basic_string< char > > const& v =
        o._user_id;

      bool is_null (true);
      std::size_t size (0);
      std::size_t cap (i._user_id_value.capacity ());
      mysql::value_traits<
          ::odb::nullable< ::std::basic_string< char > >,
          mysql::id_string >::set_image (
        i._user_id_value,
        size,
        is_null,
        v);
      i._user_id_null = is_null;
      i._user_id_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i._user_id_value.capacity ());
    }

    // _upload_time
    //
    {
      ::uint64_t const& v =
        o._upload_time;

      bool is_null (false);
      mysql::value_traits<
          ::uint64_t,
          mysql::id_ulonglong >::set_image (
        i._upload_time_value, is_null, v);
      i._upload_time_null = is_null;
    }

    return grew;
  }

  void access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // _primaryKey
    //
    {
      long long unsigned int& v =
        o._primaryKey;

      mysql::value_traits<
          long long unsigned int,
          mysql::id_ulonglong >::set_value (
        v,
        i._primaryKey_value,
        i._primaryKey_null);
    }

    // _session_id
    //
    {
      ::std::string& v =
        o._session_id;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i._session_id_value,
        i._session_id_size,
        i._session_id_null);
    }

    // _user_id
    //
    {
      ::odb::nullable< ::std::basic_string< char > >& v =
        o._user_id;

      mysql::value_traits<
          ::odb::nullable< ::std::basic_string< char > >,
          mysql::id_string >::set_value (
        v,
        i._user_id_value,
        i._user_id_size,
        i._user_id_null);
    }

    // _upload_time
    //
    {
      ::uint64_t& v =
        o._upload_time;

      mysql::value_traits<
          ::uint64_t,
          mysql::id_ulonglong >::set_value (
        v,
        i._upload_time_value,
        i._upload_time_null);
    }
  }

  void access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  init (id_image_type& i, const id_type& id)
  {
    {
      bool is_null (false);
      mysql::value_traits<
          long long unsigned int,
          mysql::id_ulonglong >::set_image (
        i.id_value, is_null, id);
      i.id_null = is_null;
    }
  }

  const char access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::persist_statement[] =
  "INSERT INTO `tbl_session_meta` "
  "(`primaryKey`, "
  "`session_id`, "
  "`user_id`, "
  "`upload_time`) "
  "VALUES "
  "(?, ?, ?, ?)";

  const char access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::find_statement[] =
  "SELECT "
  "`tbl_session_meta`.`primaryKey`, "
  "`tbl_session_meta`.`session_id`, "
  "`tbl_session_meta`.`user_id`, "
  "`tbl_session_meta`.`upload_time` "
  "FROM `tbl_session_meta` "
  "WHERE `tbl_session_meta`.`primaryKey`=?";

  const char access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::update_statement[] =
  "UPDATE `tbl_session_meta` "
  "SET "
  "`session_id`=?, "
  "`user_id`=?, "
  "`upload_time`=? "
  "WHERE `primaryKey`=?";

  const char access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::erase_statement[] =
  "DELETE FROM `tbl_session_meta` "
  "WHERE `primaryKey`=?";

  const char access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::query_statement[] =
  "SELECT "
  "`tbl_session_meta`.`primaryKey`, "
  "`tbl_session_meta`.`session_id`, "
  "`tbl_session_meta`.`user_id`, "
  "`tbl_session_meta`.`upload_time` "
  "FROM `tbl_session_meta`";

  const char access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::erase_query_statement[] =
  "DELETE FROM `tbl_session_meta`";

  const char access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::table_name[] =
  "`tbl_session_meta`";

  void access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  persist (database& db, object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    im._primaryKey_value = 0;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    {
      id_image_type& i (sts.id_image ());
      binding& b (sts.id_image_binding ());
      if (i.version != sts.id_image_version () || b.version == 0)
      {
        bind (b.bind, i);
        sts.id_image_version (i.version);
        b.version++;
      }
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    obj._primaryKey = id (sts.id_image ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;
    using mysql::update_statement;

    callback (db, obj, callback_event::pre_update);

    mysql::transaction& tr (mysql::transaction::current ());
    mysql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj._primaryKey);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  erase (database& db, const id_type& id)
  {
    using namespace mysql;

    ODB_POTENTIALLY_UNUSED (db);

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::pointer_type
  access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  find (database& db, const id_type& id)
  {
    using namespace mysql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  reload (database& db, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj._primaryKey);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace mysql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::object_type >
  access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  query (database&, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        false,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) mysql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }

  // suiSessionPtr
  //

  bool access::view_traits_impl< ::suiDataSql::suiSessionPtr, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // session
    //
    if (object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::grow (
          i.session_value, t + 0UL))
      grew = true;

    return grew;
  }

  void access::view_traits_impl< ::suiDataSql::suiSessionPtr, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i)
  {
    using namespace mysql;

    mysql::statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    std::size_t n (0);

    // session
    //
    object_traits_impl< ::suiDataSql::suiSessionMeta, id_mysql >::bind (
      b + n, i.session_value, sk);
    n += 4UL;
  }

  void access::view_traits_impl< ::suiDataSql::suiSessionPtr, id_mysql >::
  init (view_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    // session pre
    //
    typedef ::suiDataSql::suiSessionMeta session_object_type;
    typedef object_traits_impl<session_object_type, id_mysql> session_object_traits;
    typedef session_object_traits::pointer_type session_pointer_type;
    typedef session_object_traits::pointer_traits session_pointer_traits;
    typedef session_object_traits::pointer_cache_traits session_cache_traits;

    session_object_traits::id_type session_id;
    session_pointer_type session_p = 0;
    session_pointer_traits::guard session_pg;
    session_cache_traits::insert_guard session_ig;
    session_object_type* session_o (0);

    {
      if (!(i.session_value._primaryKey_null))
      {
        session_id = session_object_traits::id (i.session_value);
        session_p = session_cache_traits::find (*db, session_id);

        if (session_pointer_traits::null_ptr (session_p))
        {
          session_p = object_factory<session_object_type, session_pointer_type>::create ();
          session_pg.reset (session_p);
          session_ig.reset (session_cache_traits::insert (*db, session_id, session_p));
          session_o = session_pointer_traits::get_ptr (session_p);
        }
      }
    }

    // session
    //
    {
      if (session_o != 0)
      {
        session_object_traits::callback (*db, *session_o, callback_event::pre_load);
        session_object_traits::init (*session_o, i.session_value, db);
        session_object_traits::statements_type& sts (
          conn.statement_cache ().find_object<session_object_type> ());
        session_object_traits::load_ (sts, *session_o, false);
      }
    }

    // session post
    //
    {
      if (session_o != 0)
      {
        session_object_traits::callback (*db, *session_o, callback_event::post_load);
        session_cache_traits::load (session_ig.position ());
        session_ig.release ();
        session_pg.release ();
      }

      // If a compiler error points to the line below, then
      // it most likely means that a pointer used in view
      // member cannot be initialized from an object pointer.
      //
      o.session = ::std::shared_ptr< ::suiDataSql::suiSessionMeta > (
        std::move (session_p));
    }
  }

  access::view_traits_impl< ::suiDataSql::suiSessionPtr, id_mysql >::query_base_type
  access::view_traits_impl< ::suiDataSql::suiSessionPtr, id_mysql >::
  query_statement (const query_base_type& q)
  {
    query_base_type r (
      "SELECT "
      "`tbl_session_meta`.`primaryKey`, "
      "`tbl_session_meta`.`session_id`, "
      "`tbl_session_meta`.`user_id`, "
      "`tbl_session_meta`.`upload_time` ");

    r += "FROM `tbl_session_meta`";

    query_base_type c (
      // From data.hpp:55:44
      (q.empty () ? query_base_type::true_expr : q));

    c.optimize ();

    if (!c.empty ())
    {
      r += " ";
      r += c.clause_prefix ();
      r += c;
    }

    return r;
  }

  result< access::view_traits_impl< ::suiDataSql::suiSessionPtr, id_mysql >::view_type >
  access::view_traits_impl< ::suiDataSql::suiSessionPtr, id_mysql >::
  query (database&, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_view<view_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.image_binding ());

    if (im.version != sts.image_version () || imb.version == 0)
    {
      bind (imb.bind, im);
      sts.image_version (im.version);
      imb.version++;
    }

    const query_base_type& qs (query_statement (q));
    qs.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        qs.clause (),
        false,
        true,
        qs.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::view_result_impl<view_type> > r (
      new (shared) mysql::view_result_impl<view_type> (
        qs, st, sts, 0));

    return result<view_type> (r);
  }

  // suiFileMeta
  //

  struct access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      mysql::connection&,
      image_type&,
      id_image_type&,
      mysql::binding&,
      mysql::binding&)
    {
    }
  };

  access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::id_type
  access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  id (const id_image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          long long unsigned int,
          mysql::id_ulonglong >::set_value (
        id,
        i.id_value,
        i.id_null);
    }

    return id;
  }

  access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::id_type
  access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  id (const image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          long long unsigned int,
          mysql::id_ulonglong >::set_value (
        id,
        i._primaryKey_value,
        i._primaryKey_null);
    }

    return id;
  }

  bool access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // _primaryKey
    //
    t[0UL] = 0;

    // _file_id
    //
    if (t[1UL])
    {
      i._file_id_value.capacity (i._file_id_size);
      grew = true;
    }

    // _upload_user_id
    //
    if (t[2UL])
    {
      i._upload_user_id_value.capacity (i._upload_user_id_size);
      grew = true;
    }

    // _path
    //
    if (t[3UL])
    {
      i._path_value.capacity (i._path_size);
      grew = true;
    }

    // _size
    //
    t[4UL] = 0;

    // _mime_type
    //
    if (t[5UL])
    {
      i._mime_type_value.capacity (i._mime_type_size);
      grew = true;
    }

    // _upload_time
    //
    t[6UL] = 0;

    // _file_status
    //
    t[7UL] = 0;

    return grew;
  }

  void access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    std::size_t n (0);

    // _primaryKey
    //
    if (sk != statement_update)
    {
      b[n].buffer_type = MYSQL_TYPE_LONGLONG;
      b[n].is_unsigned = 1;
      b[n].buffer = &i._primaryKey_value;
      b[n].is_null = &i._primaryKey_null;
      n++;
    }

    // _file_id
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i._file_id_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i._file_id_value.capacity ());
    b[n].length = &i._file_id_size;
    b[n].is_null = &i._file_id_null;
    n++;

    // _upload_user_id
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i._upload_user_id_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i._upload_user_id_value.capacity ());
    b[n].length = &i._upload_user_id_size;
    b[n].is_null = &i._upload_user_id_null;
    n++;

    // _path
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i._path_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i._path_value.capacity ());
    b[n].length = &i._path_size;
    b[n].is_null = &i._path_null;
    n++;

    // _size
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i._size_value;
    b[n].is_null = &i._size_null;
    n++;

    // _mime_type
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i._mime_type_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i._mime_type_value.capacity ());
    b[n].length = &i._mime_type_size;
    b[n].is_null = &i._mime_type_null;
    n++;

    // _upload_time
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i._upload_time_value;
    b[n].is_null = &i._upload_time_null;
    n++;

    // _file_status
    //
    b[n].buffer_type = MYSQL_TYPE_LONG;
    b[n].is_unsigned = 0;
    b[n].buffer = &i._file_status_value;
    b[n].is_null = &i._file_status_null;
    n++;
  }

  void access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  bind (MYSQL_BIND* b, id_image_type& i)
  {
    std::size_t n (0);
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i.id_value;
    b[n].is_null = &i.id_null;
  }

  bool access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  init (image_type& i,
        const object_type& o,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    bool grew (false);

    // _primaryKey
    //
    if (sk == statement_insert)
    {
      long long unsigned int const& v =
        o._primaryKey;

      bool is_null (false);
      mysql::value_traits<
          long long unsigned int,
          mysql::id_ulonglong >::set_image (
        i._primaryKey_value, is_null, v);
      i._primaryKey_null = is_null;
    }

    // _file_id
    //
    {
      ::std::string const& v =
        o._file_id;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i._file_id_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i._file_id_value,
        size,
        is_null,
        v);
      i._file_id_null = is_null;
      i._file_id_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i._file_id_value.capacity ());
    }

    // _upload_user_id
    //
    {
      ::std::string const& v =
        o._upload_user_id;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i._upload_user_id_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i._upload_user_id_value,
        size,
        is_null,
        v);
      i._upload_user_id_null = is_null;
      i._upload_user_id_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i._upload_user_id_value.capacity ());
    }

    // _path
    //
    {
      ::odb::nullable< ::std::basic_string< char > > const& v =
        o._path;

      bool is_null (true);
      std::size_t size (0);
      std::size_t cap (i._path_value.capacity ());
      mysql::value_traits<
          ::odb::nullable< ::std::basic_string< char > >,
          mysql::id_string >::set_image (
        i._path_value,
        size,
        is_null,
        v);
      i._path_null = is_null;
      i._path_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i._path_value.capacity ());
    }

    // _size
    //
    {
      ::size_t const& v =
        o._size;

      bool is_null (false);
      mysql::value_traits<
          ::size_t,
          mysql::id_ulonglong >::set_image (
        i._size_value, is_null, v);
      i._size_null = is_null;
    }

    // _mime_type
    //
    {
      ::odb::nullable< ::std::basic_string< char > > const& v =
        o._mime_type;

      bool is_null (true);
      std::size_t size (0);
      std::size_t cap (i._mime_type_value.capacity ());
      mysql::value_traits<
          ::odb::nullable< ::std::basic_string< char > >,
          mysql::id_string >::set_image (
        i._mime_type_value,
        size,
        is_null,
        v);
      i._mime_type_null = is_null;
      i._mime_type_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i._mime_type_value.capacity ());
    }

    // _upload_time
    //
    {
      ::uint64_t const& v =
        o._upload_time;

      bool is_null (false);
      mysql::value_traits<
          ::uint64_t,
          mysql::id_ulonglong >::set_image (
        i._upload_time_value, is_null, v);
      i._upload_time_null = is_null;
    }

    // _file_status
    //
    {
      int const& v =
        o._file_status;

      bool is_null (false);
      mysql::value_traits<
          int,
          mysql::id_long >::set_image (
        i._file_status_value, is_null, v);
      i._file_status_null = is_null;
    }

    return grew;
  }

  void access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // _primaryKey
    //
    {
      long long unsigned int& v =
        o._primaryKey;

      mysql::value_traits<
          long long unsigned int,
          mysql::id_ulonglong >::set_value (
        v,
        i._primaryKey_value,
        i._primaryKey_null);
    }

    // _file_id
    //
    {
      ::std::string& v =
        o._file_id;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i._file_id_value,
        i._file_id_size,
        i._file_id_null);
    }

    // _upload_user_id
    //
    {
      ::std::string& v =
        o._upload_user_id;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i._upload_user_id_value,
        i._upload_user_id_size,
        i._upload_user_id_null);
    }

    // _path
    //
    {
      ::odb::nullable< ::std::basic_string< char > >& v =
        o._path;

      mysql::value_traits<
          ::odb::nullable< ::std::basic_string< char > >,
          mysql::id_string >::set_value (
        v,
        i._path_value,
        i._path_size,
        i._path_null);
    }

    // _size
    //
    {
      ::size_t& v =
        o._size;

      mysql::value_traits<
          ::size_t,
          mysql::id_ulonglong >::set_value (
        v,
        i._size_value,
        i._size_null);
    }

    // _mime_type
    //
    {
      ::odb::nullable< ::std::basic_string< char > >& v =
        o._mime_type;

      mysql::value_traits<
          ::odb::nullable< ::std::basic_string< char > >,
          mysql::id_string >::set_value (
        v,
        i._mime_type_value,
        i._mime_type_size,
        i._mime_type_null);
    }

    // _upload_time
    //
    {
      ::uint64_t& v =
        o._upload_time;

      mysql::value_traits<
          ::uint64_t,
          mysql::id_ulonglong >::set_value (
        v,
        i._upload_time_value,
        i._upload_time_null);
    }

    // _file_status
    //
    {
      int& v =
        o._file_status;

      mysql::value_traits<
          int,
          mysql::id_long >::set_value (
        v,
        i._file_status_value,
        i._file_status_null);
    }
  }

  void access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  init (id_image_type& i, const id_type& id)
  {
    {
      bool is_null (false);
      mysql::value_traits<
          long long unsigned int,
          mysql::id_ulonglong >::set_image (
        i.id_value, is_null, id);
      i.id_null = is_null;
    }
  }

  const char access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::persist_statement[] =
  "INSERT INTO `tbl_file_meta` "
  "(`primaryKey`, "
  "`file_id`, "
  "`upload_user_id`, "
  "`path`, "
  "`size`, "
  "`mime_type`, "
  "`upload_time`, "
  "`file_status`) "
  "VALUES "
  "(?, ?, ?, ?, ?, ?, ?, ?)";

  const char access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::find_statement[] =
  "SELECT "
  "`tbl_file_meta`.`primaryKey`, "
  "`tbl_file_meta`.`file_id`, "
  "`tbl_file_meta`.`upload_user_id`, "
  "`tbl_file_meta`.`path`, "
  "`tbl_file_meta`.`size`, "
  "`tbl_file_meta`.`mime_type`, "
  "`tbl_file_meta`.`upload_time`, "
  "`tbl_file_meta`.`file_status` "
  "FROM `tbl_file_meta` "
  "WHERE `tbl_file_meta`.`primaryKey`=?";

  const char access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::update_statement[] =
  "UPDATE `tbl_file_meta` "
  "SET "
  "`file_id`=?, "
  "`upload_user_id`=?, "
  "`path`=?, "
  "`size`=?, "
  "`mime_type`=?, "
  "`upload_time`=?, "
  "`file_status`=? "
  "WHERE `primaryKey`=?";

  const char access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::erase_statement[] =
  "DELETE FROM `tbl_file_meta` "
  "WHERE `primaryKey`=?";

  const char access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::query_statement[] =
  "SELECT "
  "`tbl_file_meta`.`primaryKey`, "
  "`tbl_file_meta`.`file_id`, "
  "`tbl_file_meta`.`upload_user_id`, "
  "`tbl_file_meta`.`path`, "
  "`tbl_file_meta`.`size`, "
  "`tbl_file_meta`.`mime_type`, "
  "`tbl_file_meta`.`upload_time`, "
  "`tbl_file_meta`.`file_status` "
  "FROM `tbl_file_meta`";

  const char access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::erase_query_statement[] =
  "DELETE FROM `tbl_file_meta`";

  const char access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::table_name[] =
  "`tbl_file_meta`";

  void access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  persist (database& db, object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    im._primaryKey_value = 0;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    {
      id_image_type& i (sts.id_image ());
      binding& b (sts.id_image_binding ());
      if (i.version != sts.id_image_version () || b.version == 0)
      {
        bind (b.bind, i);
        sts.id_image_version (i.version);
        b.version++;
      }
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    obj._primaryKey = id (sts.id_image ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;
    using mysql::update_statement;

    callback (db, obj, callback_event::pre_update);

    mysql::transaction& tr (mysql::transaction::current ());
    mysql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj._primaryKey);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  erase (database& db, const id_type& id)
  {
    using namespace mysql;

    ODB_POTENTIALLY_UNUSED (db);

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::pointer_type
  access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  find (database& db, const id_type& id)
  {
    using namespace mysql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  reload (database& db, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj._primaryKey);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace mysql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::object_type >
  access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  query (database&, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        false,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) mysql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::suiDataSql::suiFileMeta, id_mysql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }
}

namespace odb
{
  static bool
  create_schema (database& db, unsigned short pass, bool drop)
  {
    ODB_POTENTIALLY_UNUSED (db);
    ODB_POTENTIALLY_UNUSED (pass);
    ODB_POTENTIALLY_UNUSED (drop);

    if (drop)
    {
      switch (pass)
      {
        case 1:
        {
          return true;
        }
        case 2:
        {
          db.execute ("DROP TABLE IF EXISTS `tbl_file_meta`");
          db.execute ("DROP TABLE IF EXISTS `tbl_session_meta`");
          return false;
        }
      }
    }
    else
    {
      switch (pass)
      {
        case 1:
        {
          db.execute ("CREATE TABLE `tbl_session_meta` (\n"
                      "  `primaryKey` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,\n"
                      "  `session_id` VARCHAR(128) NOT NULL,\n"
                      "  `user_id` VARCHAR(128) NULL,\n"
                      "  `upload_time` BIGINT UNSIGNED NOT NULL DEFAULT 0 NOT NULL)\n"
                      " ENGINE=InnoDB");
          db.execute ("CREATE UNIQUE INDEX `session_id_i`\n"
                      "  ON `tbl_session_meta` (`session_id`)");
          db.execute ("CREATE INDEX `user_id_i`\n"
                      "  ON `tbl_session_meta` (`user_id`)");
          db.execute ("CREATE TABLE `tbl_file_meta` (\n"
                      "  `primaryKey` BIGINT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,\n"
                      "  `file_id` VARCHAR(128) NOT NULL,\n"
                      "  `upload_user_id` VARCHAR(128) NOT NULL,\n"
                      "  `path` VARCHAR(512) NULL,\n"
                      "  `size` BIGINT UNSIGNED NOT NULL,\n"
                      "  `mime_type` VARCHAR(64) NULL,\n"
                      "  `upload_time` BIGINT UNSIGNED NOT NULL DEFAULT 0 NOT NULL,\n"
                      "  `file_status` INT NOT NULL)\n"
                      " ENGINE=InnoDB");
          db.execute ("CREATE UNIQUE INDEX `file_id_i`\n"
                      "  ON `tbl_file_meta` (`file_id`)");
          db.execute ("CREATE INDEX `upload_user_id_i`\n"
                      "  ON `tbl_file_meta` (`upload_user_id`)");
          return false;
        }
      }
    }

    return false;
  }

  static const schema_catalog_create_entry
  create_schema_entry_ (
    id_mysql,
    "",
    &create_schema);
}

#include <odb/post.hxx>
